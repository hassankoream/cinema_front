import { TrimFirstPipe } from './trim-first.pipe';

describe('TrimFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
