import { SplitTitlePipe } from './split-title.pipe';

describe('SplitTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
